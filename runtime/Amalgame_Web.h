/*
 * Amalgame.Web — Mosaic web framework runtime header.
 * Copyright (c) 2026 Bastien MOUGET
 * Licensed under the Apache License, Version 2.0.
 *
 * v0.1: pure-Amalgame implementation in `facade.am`. This stub
 * exists because the package manifest's [stdlib].header field
 * is currently required by amc's PackageRegistry.LoadFrom.
 *
 * Future v0.x will add C-side helpers here for things that need
 * native code: filesystem watcher integration (DEV mode), worker
 * thread pool, signal handling for graceful shutdown.
 */

#ifndef AMALGAME_WEB_H
#define AMALGAME_WEB_H

#include "_runtime.h"

#endif /* AMALGAME_WEB_H */
